/* Author: Jenner Hanni
 * Language: C
 * Purpose: To write insertion and merge sorts.
 * License: MIT
 */

#include <stdio.h>

int main() {

  int an_array[] = {1,2,9,4,8,5,6,3,8,1};

  int i;
  int array_length = sizeof(an_array)/sizeof(int);
  int int_length = sizeof(int);

  printf("sizeof(int): %d\n",int_length);
  printf("array_length: %d\n",array_length);

  return 0;
}
